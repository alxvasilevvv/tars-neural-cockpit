/*
 * Entry script for hero.html — marketing hero ported from the W307
 * reference. Same contract as cockpit-entry.ts: HTML carries the
 * visual truth, this file just wires the shared design tokens into
 * the Vite graph.
 */

import "../styles/global.css";
