/*
 * Entry script for index.html — the cockpit landing / page picker.
 *
 * This page intentionally has no JS behaviour; importing the global
 * stylesheet here is enough to wire the design tokens into Vite's
 * dependency graph so production builds emit `cockpit-<hash>.css`.
 */

import "../styles/global.css";
