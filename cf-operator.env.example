# 1) cp cf-operator.env.example cf-operator.env
# 2) Дописай значения после = (токен только на одной строке с ключом)
# 3) make ops-cf-pages-token
# Обход .env: скопируй токен в буфер →  pbpaste | tr -d '\n\r' | gh secret set CLOUDFLARE_API_TOKEN -R alxvasilevvv/tars-neural-cockpit

CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
