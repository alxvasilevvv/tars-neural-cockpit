# TARS v10.0 — Performance Report

> Auto-generated by `scripts/RUN-PERF-SUITE.command`. The committed
> copy is a **placeholder** — run the suite on the GA-target host
> (Alien's main machine) and the script will overwrite this file with
> the live p50/p95/p99 numbers and PASS/FAIL verdict.

## Summary

| Bench | SLO target | p50 | p95 | p99 | Result |
|---|---|---:|---:|---:|:--:|
| `bench_chat` | p95 < 2.5s (100 concurrent) | — | — | — | NOT RUN |
| `bench_voice_command` | p95 < 800ms (50 concurrent) | — | — | — | NOT RUN |
| `bench_usage_metering` | 1000/s sustained, no drops | — | — | — | NOT RUN |
| `bench_audit_timeline` | p95 < 200ms (10k receipts) | — | — | — | NOT RUN |
| `bench_composer_plan` | p95 < 4s (20 concurrent) | — | — | — | NOT RUN |

## How to refresh

```bash
bash scripts/RUN-PERF-SUITE.command
```

The runner wipes `tests/perf/.results/`, fires the five `tests/perf/bench_*.py`
files via `pytest -m perf`, then re-renders this document with the
real numbers. Aborts with `exit 1` if any SLO is breached and prints
the suggested fix from the failing bench's assertion message.

## SLO source of truth

| Bench | Why this SLO | Where defined |
|---|---|---|
| `bench_chat` | Voice cockpit feels live below 2.5s p95; above that, users start re-asking. | `tests/perf/bench_chat.py:SLO_MS` |
| `bench_voice_command` | Regex-hit path must beat human reaction time (~800ms p95). | `tests/perf/bench_voice_command.py:SLO_MS` |
| `bench_usage_metering` | 1000/s = 2x the worst observed cockpit burst rate. | `tests/perf/bench_usage_metering.py:TARGET_PER_S` |
| `bench_audit_timeline` | Audit tab filter change feels instant only below 200ms. | `tests/perf/bench_audit_timeline.py:SLO_MS` |
| `bench_composer_plan` | A 20-file voice refactor must plan in under 4s. | `tests/perf/bench_composer_plan.py:SLO_MS` |
