# Vite bake-time for prod build — same-origin API served under this host unless you split edge vs daemon.
# Override if the FastAPI daemon is deployed on a different origin (ensure CORS/env there too).
VITE_TARS_API=https://tars.meeet.world
