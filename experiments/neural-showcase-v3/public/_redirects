# Cloudflare Pages redirects — first match wins.
# Format: https://developers.cloudflare.com/pages/configuration/redirects/

# ----- Permanent redirects (legacy paths) -----
/home              /                  301
/sign-up           /onboarding        301
/signup            /onboarding        301
/get-started       /onboarding        301

# ----- B-001 legacy installer URLs (302 → GitHub; same contract as meeet.world) -----
# Human-friendly install page remains at /install (SPA). curl | sh MUST use /install.sh.
/dl/TARS-8.4.0-arm64.dmg   https://github.com/alxvasilevvv/tars-neural-cockpit/releases/download/v8.4.0/TARS_8.4.0_aarch64.dmg   302
/dl/TARS-8.4.0-setup.exe   https://github.com/alxvasilevvv/tars-neural-cockpit/releases/download/v8.4.0/TARS_8.4.0_x64-setup.exe   302
/dl/TARS-8.4.0.AppImage    https://github.com/alxvasilevvv/tars-neural-cockpit/releases/download/v8.4.0/TARS_8.4.0_amd64.AppImage  302
/install.sh                https://raw.githubusercontent.com/alxvasilevvv/tars-neural-cockpit/main/scripts/install-tars.sh          302

# ----- Downloads manifest -----
# `/api/product/downloads` and `/api/product/version` are served by
# Pages Functions (`functions/api/product/{downloads,version}.ts`) so
# they live on the same origin as the SPA. Cloudflare Pages does not
# allow `_redirects` rewrites (status 200) to external origins, which
# is why the external proxy lives in code rather than here.

# ----- SPA fallback — must be last -----
# All other paths render index.html so client router takes over.
/*                 /index.html        200
